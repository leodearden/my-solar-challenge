# Core dependencies (matches pyproject.toml [project.dependencies])
pvlib>=0.10.0
pandas>=2.0.0
numpy>=1.24.0
pyyaml>=6.0.0
typer[all]>=0.9.0

# Dev dependencies (matches pyproject.toml [project.optional-dependencies.dev])
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# or: pip install -e ".[dev]"
pytest>=7.0.0
pytest-cov>=4.0.0
mypy>=1.0.0
